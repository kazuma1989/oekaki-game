{
  "extends": "./tsconfig",

  "compilerOptions": {
    // Redirect output structure to a directory.
    // The directory is defined in the package.json#scripts.
    "noEmit": false
  },

  "exclude": ["**/test.*", "**/*.test.*"]
}
