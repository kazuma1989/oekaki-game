{
  "name": "oekaki-game",
  "version": "1.0.0",
  "repository": "git@github.com:kazuma1989/oekaki-game.git",
  "author": "kazuma1989 <kazuma.ebina@quipper.com>",
  "license": "MIT",
  "scripts": {
    "prepare": "snowpack --clean --dest ./public/web_modules/",
    "start": "concurrently npm:serve npm:build-watch",
    "serve": "live-server ./public",
    "build": "tsc --project ./tsconfig.build.json",
    "build-watch": "tsc --project ./tsconfig.build.json --watch",
    "test": "tsc"
  },
  "dependencies": {
    "immer": "^5.3.2",
    "preact": "^10.2.1",
    "redux": "^4.0.5"
  },
  "devDependencies": {
    "@types/redux": "^3.6.0",
    "concurrently": "^5.0.2",
    "live-server": "^1.2.1",
    "snowpack": "^1.1.0",
    "typescript": "^3.7.5"
  },
  "snowpack": {
    "webDependencies": [
      "immer",
      "preact",
      "preact/hooks",
      "redux"
    ]
  }
}
